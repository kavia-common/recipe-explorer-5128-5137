# Base URL for the backend API (optional). If not set, the app will use local sample data.
# Example: https://api.example.com
REACT_APP_API_BASE_URL=

# Optional: Site name to display in the header and document title.
REACT_APP_SITE_NAME=Recipe Explorer
